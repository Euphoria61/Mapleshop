<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.euphoria.shop.mapper.MenuMapper">
    <select id="selectPermission" resultType="java.lang.String">
        SELECT  DISTINCT p.permiss_name
        FROM user_role ur
                 LEFT JOIN role r ON ur.role_id =r.sys_role_id
                 LEFT JOIN role_permisson rp ON ur.role_id =rp.role_id
                 LEFT JOIN permisson p ON rp.permission_id =p.permission_id
        WHERE ur.user_id = #{userId}
    </select>
</mapper>